# 5주차 측정 결과 기록표

> 실습 2·4의 측정 결과를 여기에 기록해 `week05/` 폴더와 함께 커밋합니다.
> 실습 과제 제출물의 일부입니다.

- 이름 / 학번 :
- 실행 환경 : 모델 `gemma3:4b` / GPU
- 실행 날짜 :

---

## 실습 2 — 파싱 실패율 A/B (`ab_failrate.py`)

실행 조건: `N = 20`, `temperature = 0.7` (**A·B 동일** ★)

| 방법 | 실패 횟수 | 실패율 | 주된 실패 사유 |
|---|---:|---:|---|
| **A. "JSON으로 답해줘"** | ___ / 20 | ___ % | |
| **B. `with_structured_output()`** | ___ / 20 | ___ % | |

**A의 실패 사유별 내역** (앞말 / 코드펜스 / 타입 / 필드명 / 범위)

| 사유 | 횟수 |
|---|---:|
| JSON 파싱 실패 (앞말·코드펜스) | |
| 스키마 불일치 (타입·필드명·범위) | |

**B가 0%가 아니었다면, 그 이유는?**

>

**결론 한 문장으로**

> 프롬프트를 강하게 쓰는 것으로는 0%에 못 간다.
> 형식은 프롬프트가 아니라 ________________ 이 지켜야 한다.

---

## 실습 4 — Self-Consistency 표 분포 (`self_consistency.py`)

사용한 문제:

>

| 항목 | 값 |
|---|---|
| `temperature = 0` 일 때 표 분포 | ______ (예: `{'17': 5}` — 만장일치) |
| `temperature = 0.8` 일 때 표 분포 | ______ (예: `{'17': 4, '23': 1}`) |
| 1회만 실행했다면 나왔을 답 | ______ |
| 다수결로 정한 답 | ______ |
| N=5 실행에 걸린 시간 | ______ 초 |

**`temperature = 0` 에서 만장일치가 나온 이유를 한 줄로**

>

**다수결이 걸러내지 못하는 실패는 무엇인가**

>

---

## 실습 3 — Least-to-Most 호출 횟수 (`least_to_most.py`)

| 항목 | 값 |
|---|---:|
| 생성된 하위 질문 개수 | |
| 총 LLM 호출 횟수 (분해 1 + 하위 n + 종합 1) | |
| 단일 프롬프트 1회 대비 배수 | |

**쪼갠 답과 한 번에 물은 답의 차이 (한 줄)**

>

> 📌 이 체인이 **6주차 [과제 2]** 의 대상입니다. 반드시 커밋해 두십시오.
