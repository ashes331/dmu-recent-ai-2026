# 6주차 추적 분석 기록표

> 2교시 실습 2에서 화면을 보며 **그 자리에서 채웁니다.**
> **이 표가 그대로 [과제 2] 분석 보고서의 뼈대입니다.** ★
> `week06/` 폴더와 함께 커밋합니다.

- 이름 / 학번 :
- 실행 환경 : 모델 `gemma3:4b` / GPU
- 실행 날짜 :
- LangSmith 프로젝트 : `week06-tracing`

> ⚠️ **추적 보존은 14일입니다.** 오늘(10/7) 만든 추적은 **10/21 무렵 사라집니다.**
> 표를 채우는 것과 **별개로 캡처를 지금 저장**하십시오. 나중엔 화면이 없습니다. ★

---

## 0. 연동 확인 (1교시 실습 1)

| 확인 항목 | 결과 |
|---|---|
| `python check_langsmith.py` 전부 `[O]` | ☐ |
| `python trace_on.py` 실행 후 목록에 기록됨 | ☐ |
| `git check-ignore -v .env` 출력 있음 (= 안전) ★ | ☐ |
| ⚠️ 개인 신용카드를 등록하지 **않았음** | ☐ |

---

## 1. 트리 세어 보기 (2교시 2-1)

`trace_on.py` 의 Trace 하나를 열고 왼쪽 트리를 **끝까지 펼쳐서** 셉니다.

| 확인 | 내 화면의 값 |
|---|---|
| 이 Trace의 **Run 총 개수** | ______ 개 |
| 그중 **`llm` 타입 Run 개수** ★ | ______ 개 |
| 트리의 **최대 깊이** | ______ 단 |

**5주차에서 계산했던 호출 횟수(분해 1 + 하위 3~4 + 종합 1 = 5~6회)와 맞습니까?**

>

---

## 2. 숨은 프롬프트 확인 (2교시 2-2 ①) ★★

**세 번째 하위 질문**의 `ChatPromptTemplate` Run 을 열어 Output 을 봅니다.

| 항목 | 값 |
|---|---|
| 코드에 적혀 있던 것 | `{solved}` |
| 모델이 **실제로 받은** 문자열의 길이 | 약 ______ 자 |
| 하위 질문이 10개였다면 어떻게 되겠는가 | |

>

---

## 3. 단계별 기록표 (2교시 2-4) ★★ — 보고서 ②번 항목

| Run (단계) | 타입 | 지연(초) | 입력 토큰 | 출력 토큰 | 비고 |
|---|---|---:|---:|---:|---|
| 분해기 | `llm` | | | | |
| 하위질문 1 | `llm` | | | | |
| 하위질문 2 | `llm` | | | | |
| 하위질문 3 | `llm` | | | | |
| 하위질문 4 | `llm` | | | | |
| 종합 | `llm` | | | | |
| **합계** | | | | | |

> 🔶 토큰 수가 화면에 안 나오면 **"측정 불가"** 로 적으십시오.
> 공급자·버전에 따라 비어 있을 수 있습니다. (4주차 `usage_metadata` 와 같은 이슈)
> 로컬 모델은 **Cost 가 0 또는 공란**입니다 — 당연합니다, 내 GPU를 썼으니까요.

**표를 채우면서 답할 질문 3개** ★

① 가장 오래 걸린 단계는?

>

② 토큰을 가장 많이 쓴 단계는? 왜 그 단계인가?

>

③ **뒤로 갈수록 입력 토큰이 늘어나는가? 왜인가?** ★

>

---

## 4. `batch()` 의 추적 모양 (2교시 2-3)

5주차 `self_consistency.py` 를 **추적을 켠 채로** 실행합니다.

| 항목 | 값 |
|---|---|
| `batch(5)` 로 생긴 **Trace 개수** ⚠️ | ______ 건 |
| 입력이 전부 같은가 | |
| 출력이 서로 다른가 (= `temperature > 0` 의 증거) ★ | |
| 소수 의견 Run 의 `reasoning` — **어디서 미끄러졌는가** ★★ | |

>

> ⚠️ `batch(N)` = **N traces**. 한도 소모에 주의. 실습 중에는 **N=5, 실행 2회 이내**.

---

## 5. 병목 진단 (2교시 3절) ★ — 보고서 ①번 항목

> ⚠️ **부모 Run을 병목으로 적으면 감점 소지가 있습니다.**
> 부모는 자식 시간을 **포함한 합계**일 뿐입니다.
> **잎 Run(자식이 없는 Run)만 비교**하십시오.

| 항목 | 값 |
|---|---|
| 루트 Run 의 전체 시간 | ______ 초 |
| **가장 오래 걸린 잎 Run** ★ | |
| 그 Run 의 지연 | ______ 초 |
| 전체 대비 비율 | ______ % |

**왜 그 단계가 느렸는가 — 근거를 적으십시오** ★

| 후보 원인 | 해당? | 근거 (화면에서 읽은 숫자) |
|---|---|---|
| 출력 토큰이 많다 (답을 길게 씀) | ☐ | |
| 입력 토큰이 많다 (누적된 컨텍스트) | ☐ | |
| **토큰은 적은데 느리다** → 모델 로딩·CPU 분산 ★ | ☐ | `ollama ps` 의 `PROCESSOR` = |

> ★ **워밍업 확인**: 같은 체인을 **두 번 실행**해 두 Trace 를 비교하십시오.
> 1회차만 유독 느렸다면 그건 **모델 문제가 아니라 로딩**입니다.
>
> | | 1회차 | 2회차 |
> |---|---:|---:|
> | 첫 `llm` Run 의 지연 | ______ 초 | ______ 초 |

---

## 6. 지연 ≠ 비용 (2교시 3-4)

| 정렬 기준 | 1등이 된 단계 | 무엇을 개선하게 되나 |
|---|---|---|
| **지연시간** | | 체감 속도 (사용자 경험) |
| **토큰 사용량** | | 비용 (호출당 단가) |

**둘이 달랐습니까? 다르다면 왜입니까?**

>

---

## 7. 개선 아이디어 (보고서 ③번 항목) ★

> ★ **무엇을 잃는지도 함께 쓰십시오. 대가 없는 개선은 없습니다.**
> 실제 측정은 **7주차**에 합니다 — 지금은 **가설까지**입니다.

| 아이디어 | 무엇이 좋아지나 | **무엇을 잃나** ★ |
|---|---|---|
| | | |
| | | |
| | | |

<details>
<summary>예시 (그대로 베끼지 말고 내 추적을 근거로 쓸 것)</summary>

| 아이디어 | 무엇이 좋아지나 | 무엇을 잃나 |
|---|---|---|
| 하위 질문을 4개 → 3개로 | 호출 1회 감소 = 시간·비용 | 분해가 거칠어질 수 있음 |
| `solved` 를 요약해서 넘김 | 뒷단계 입력 토큰 감소 | 정보 손실 위험 |
| 쉬운 단계만 소형 모델로 | 비용 감소 | 품질 저하 가능 |
| 독립적인 하위 질문은 `batch` 로 | 지연 감소 | **순차 의존이 있으면 불가** ★ |

</details>

---

## 8. 3교시 실습 확인

| 실습 | 확인 항목 | 결과 |
|---|---|---|
| 실습 3 | `리뷰 전처리` Run 이 트리에 나타남 | ☐ |
| 실습 3 | `LIMIT=50` 으로 돌렸을 때 **전처리 Run 의 Output** 에 원인이 보임 ★ | ☐ |
| 실습 3 | 태그 `exp-A` 로 필터하니 그 실행만 남음 | ☐ |
| 실습 3 | 메타데이터에 `temperature` 가 기록됨 | ☐ |
| 실습 4 | Hub 에 프롬프트 등록 · **Commits 에 v1/v2 2건** | ☐ |
| 실습 4 | `pull_prompt` 로 불러온 객체를 `|` 로 그대로 이음 | ☐ |

**실습 3에서 `LIMIT=50` 으로 바꿨을 때 무엇을 보았습니까?** ★

>

---

## 9. 캡처 목록 ⚠️ 필수

> **보존 14일** — 채점 시점에 링크가 죽어 있을 수 있습니다. **캡처가 본체입니다.** ★

| # | 캡처할 화면 | 파일명 | 저장함 |
|---|---|---|---|
| 1 | **트리를 끝까지 펼친 Trace 상세** ★★ | `captures/01_tree.png` | ☐ |
| 2 | 세 번째 하위 질문의 `prompt` Run Output (숨은 프롬프트) ★ | `captures/02_rendered_prompt.png` | ☐ |
| 3 | 가장 느린 잎 Run 의 상세 (지연·토큰) | `captures/03_bottleneck.png` | ☐ |
| 4 | 태그 `exp-A` 로 필터한 목록 | `captures/04_tags.png` | ☐ |
| 5 | Prompt Hub 의 Commits 화면 | `captures/05_hub_commits.png` | ☐ |

> ⚠️ **캡처에 개인정보나 API 키가 찍히지 않았는지 확인**하고 저장하십시오.
> 무료 플랜은 **1 seat** 이라 교수 워크스페이스 초대가 불가능합니다.
> 공개 공유(Share) 링크는 **선택**, 캡처는 **필수**입니다.

---

## 오늘의 한 줄

> **재현할 수 없다면, 기록해 두는 수밖에 없다.**
>
> 그리고 — 병목은 **부모가 아니라 잎**에 있다. ★
