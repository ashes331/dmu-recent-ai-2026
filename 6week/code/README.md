# 6주차 실습 코드 — LangSmith (1) 추적과 디버깅

**2026년 10월 7일 (수) — 추석 보강일** / 최신인공지능 6주차
※ 진도 맞바꿈 적용 — 10/2(금)에 5주차 진도, **10/7(수)** 에 6주차 진도
⚠️ **요일이 수요일입니다.** 결석 위험이 가장 큰 차시입니다.

> **"`print()`로 디버깅해 보셨죠? LLM 앱에서는 그게 왜 안 통하는지 보여드립니다."**

## 파일 구성

| 실습 | 교시 | 파일 | 내용 |
|------|------|------|------|
| 사전 | — | [`net_check.py`](net_check.py) | 🔶 **교수용** — 수업 당일 아침 접속 확인 1회 |
| **실습 1** ★ | 1교시 2절 | [`check_langsmith.py`](check_langsmith.py) | `.env` 연동 확인 · 흔한 실수 3종 자동 진단 |
| **실습 1** ★★ | 1교시 2-4 | [`trace_on.py`](trace_on.py) | **코드 수정 0줄** — 5주차 체인 그대로 실행 |
| — | 1교시 2-5 | [`trace_off.py`](trace_off.py) | **끄는 법** — 반드시 시연 ⚠️ 보안 |
| 시연 | 2교시 3절 | [`slow_chain.py`](slow_chain.py) | 🔶 **교수용** — 병목이 눈에 띄는 예시 체인 · 부모 Run의 함정 ★ |
| **실습 3** ★ | 3교시 1-2 | [`traceable_custom.py`](traceable_custom.py) | **`@traceable`** — 내 함수를 트리에 올린다 |
| **실습 3** ★★ | 3교시 1-3 | [`tags_metadata.py`](tags_metadata.py) | **태그·메타데이터로 실험 구분** (7주차 A/B의 전제) |
| **실습 4** | 3교시 2절 | [`prompt_hub.py`](prompt_hub.py) | Prompt Hub 등록 · 버전 갱신 · 불러오기 |
| 기록 | 2교시 · 과제 | [`RESULTS.md`](RESULTS.md) | ★★ **기록표 = 과제 2 보고서의 뼈대** |
| 견본 | — | [`.env.example`](.env.example) | 오늘 추가되는 환경변수 3개 |

> **실습 2는 코드가 없습니다.** 브라우저에서 추적 화면을 읽는 실습입니다.
> 대신 [`RESULTS.md`](RESULTS.md) 를 **그 자리에서 채우게** 하십시오 — 그게 실습 2입니다. ★
>
> 학생 저장소에서는 `week06/` 경로에 두게 합니다.

## 사전 준비

```bash
# 5주차까지 쓰던 가상환경을 그대로 사용합니다
# Windows
venv\Scripts\activate
# macOS / Linux
# source venv/bin/activate

pip install -r requirements.txt     # 새로 설치할 것은 사실상 없습니다

cp .env.example .env                # Windows: copy .env.example .env
# .env 를 열어 LANGSMITH_API_KEY 를 채웁니다

git check-ignore -v .env            # ★ 출력이 나와야 안전합니다
```

⚠️ **`.env` 는 절대 커밋하지 않습니다.** 3주차 과제 1의 채점 항목이었고,
**오늘 키가 하나 더 늘었습니다.**

⚠️⚠️ **개인 신용카드를 절대 등록하지 마십시오.**
무료 Developer 플랜은 **카드 없이 가입되고**, 한도를 넘으면 **429로 거부될 뿐 과금되지 않습니다**(하드 캡).
카드를 등록하면 그 안전장치가 풀립니다.

## 실행 순서 (수업 진행 순서)

```bash
# ── 수업 당일 아침 (교수) ─────────────────────────────
python net_check.py            # 접속 확인 1회  🔶

# ── 1교시 : 연동 (코드 수정 0줄을 확인시키는 것이 목표) ★
python check_langsmith.py      # 실습 1 — .env 가 제대로 로드됐나
python trace_on.py             # 실습 1 ★★ — 목록에 찍히는 것까지만!
python trace_off.py            # 2-5절 — 끄는 법 시연 ⚠️ 필수

# ── 2교시 : 화면 읽기 (브라우저) ★★
#   RESULTS.md 를 열어 놓고 채우게 합니다 — 이게 실습 2입니다
python slow_chain.py 2         # 병목 시연 + 워밍업 비교 ★
python ../../5week/code/self_consistency.py   # batch() 의 추적 모양 (N=5, 2회 이내 ⚠️)

# ── 3교시 : 범위 넓히기 + 버전 관리
python traceable_custom.py     # 실습 3 ★
python traceable_custom.py 50  #   ★ 전처리 버그를 일부러 만들어 본다
python tags_metadata.py        # 실습 3 ★★ — 태그·메타데이터
python prompt_hub.py           # 실습 4  🔶 사전 검증 필수
```

## 오늘 추가되는 환경변수 3개 ★

| 변수 | 하는 일 | 안 넣으면 |
|---|---|---|
| **`LANGSMITH_TRACING`** | 추적 켜기/끄기 스위치 ★ | 추적 안 됨 |
| **`LANGSMITH_API_KEY`** | 내 계정으로 보내기 | 인증 실패 |
| `LANGSMITH_PROJECT` | 기록을 담을 프로젝트 이름 | `default` 로 들어감 |

> 🔶 **변수 이름 확인**: 예전 이름은 `LANGCHAIN_TRACING_V2` · `LANGCHAIN_API_KEY` ·
> `LANGCHAIN_PROJECT` 였습니다. 현재도 대개 함께 인식되지만,
> **수업일 기준 공식 문서에서 권장 이름을 확인**해 배포용 `.env` 를 확정하십시오. ★
> (`check_langsmith.py` 가 두 이름 모두 확인해 줍니다)

## ⚠️ 안 될 때의 점검 순서 — 미리 판서해 두십시오

| 증상 | 원인 | 조치 |
|---|---|---|
| 목록에 아무것도 없음 | `LANGSMITH_TRACING` 이 `true` 가 아님 | `python check_langsmith.py` |
| 인증 오류 | 키 앞뒤 공백·따옴표 | `.env` 에서 따옴표 없이 붙여넣기 |
| **`.env` 를 고쳤는데 안 바뀜** ★ | 셸에 기존 환경변수가 남아 있음 | **터미널을 새로 열고** 재실행 |
| `load_dotenv()` 가 아래쪽에 있음 | 라이브러리가 이미 값을 읽은 뒤 | **파일 맨 위**로 올린다 ★ |
| 접속 자체가 안 됨 | 프록시·백신 | `python net_check.py` |
| 토큰 수가 안 보임 | 공급자·버전 이슈 🔶 | **"측정 불가"** 로 처리 (4주차 `usage_metadata` 와 같은 이슈) |

## 수업 전 사전 테스트 (교수용) 🔶

- [ ] **`net_check.py`** — 당일 아침 1회. ✅ 외부망은 2026-08-19 개방 확인됨
- [ ] **`prompt_hub.py`** — ★ **이 차시 최대의 위험 지점**
      `push_prompt` / `pull_prompt` 의 인자 이름이 `langsmith` 버전에 따라 다릅니다.
      실습실 PC에서 반드시 1회 실행해 **동작하는 형태로 배포 코드를 확정**하십시오.
      여기서 막히면 3교시 2절이 통째로 멈춥니다
- [ ] **`trace_off.py`** — `tracing_context(enabled=False)` 가 실습실 버전에서 동작하는지 확인.
      ⚠️ 강의안의 `config={"callbacks": []}` 는 **버전에 따라 꺼지지 않습니다**
      (환경변수로 켜져 있으면 프레임워크가 추적기를 다시 붙임) — 어느 쪽을 가르칠지 확정할 것 ★
- [ ] **`slow_chain.py`** — ② 단계가 실제로 압도적으로 느린지 확인.
      차이가 작으면 "최소 900자" 를 1500자로 올린다
- [ ] **토큰 수 표시 확인** — `gemma3:4b` 에서 입력/출력 토큰이 화면에 나오는지.
      안 나오면 학생에게 **"측정 불가"로 적으라**고 미리 정해 둘 것 ★
- [ ] LangSmith **가입 화면·API 키 화면·Trace 상세 화면을 전날 직접 캡처**해 슬라이드에 반영
      (메뉴 이름과 배치가 서비스 업데이트로 바뀝니다. "화면이 슬라이드와 다르다" 는 질문이 나오면 차시가 느려집니다)
- [ ] 태그로 구분된 실험 2건을 미리 실행해 **비교 화면 확보** (`python tags_metadata.py`)
- [ ] Prompt Hub 예제 1건을 미리 등록해 **버전 이력 화면 확보**
- [ ] **10/7(수) 보강 재공지** ★ — 강의실·교시는 동일, **요일만 수요일**

## 주의

- 모든 파일 상단에 `MODEL = "gemma3:4b"` 상수를 두었습니다.
  실습실 모델이 다르면 **이 한 줄만** 고치면 됩니다.
- `load_dotenv()` 는 **반드시 파일 맨 위**에 둡니다. 아래에 있으면 안 먹습니다. ★
  (그래서 이 폴더의 파일들은 `import` 순서가 일반적인 관례와 다릅니다 — 의도된 것입니다)
- `trace_on.py` 는 5주차 `least_to_most.py` 를 옮긴 것입니다.
  **다른 점은 맨 위의 `load_dotenv()` 두 줄뿐**입니다.
  더 좋은 시연은 **학생의 `week05/least_to_most.py` 상단에 그 두 줄만 넣고 그대로 돌리는 것**입니다. ★
- `batch(N)` 은 **N traces** 를 소모합니다. 실습 중에는 **N=5, 실행 2회 이내**로 제한하십시오. ⚠️

## ⚠️ 무료 플랜 제약 네 가지

| 제약 | 실제로 무슨 일이 | 우리의 대응 |
|---|---|---|
| **카드 등록 불필요 (하드 캡)** | 한도 초과 시 429 거부, **과금 없음** | ⚠️ **개인 카드 절대 등록 금지** ★ |
| **1 seat** | 워크스페이스에 남을 초대 불가 | 과제는 **공유 링크 + 캡처**로 제출 |
| **추적 보존 14일** ★ | 오늘 추적은 **10/21경 소멸** | **캡처 필수**, 링크는 보조 ★★ |
| **월 5,000 traces** | 반복 실습 시 소모 | `batch(N)` 반복 자제 |

```
   chain.invoke() 1회             =  1 trace
   Least-to-Most 1회 (LLM 5~6회)  =  1 trace     ← 안에서 몇 번 부르든 1건
   batch(5) 1회                   =  5 traces ⚠️
```

## 📌 과제 2 — 오늘 부여

| # | 제출물 |
|---|--------|
| 1 | 추적이 적용된 체인 코드 (`week06/` 커밋) |
| 2 | 분석 보고서 — **병목 구간 / 토큰 사용량 / 개선 아이디어** |
| 3 | ★ **추적 화면 캡처 (필수)** — 트리가 펼쳐진 화면 |
| 4 | 공개 공유(Share) 링크 (선택) |

> [`RESULTS.md`](RESULTS.md) 를 채우면 **보고서의 절반이 이미 완성**됩니다.
> ⚠️ **부모 Run 을 병목으로 적으면 감점 소지** — 병목은 **잎 Run** 입니다. ★
> **마감**: 🔶 (확정 후 공지)

## 오늘의 한 줄

> **재현할 수 없다면, 기록해 두는 수밖에 없다.**
>
> 코드 수정 0줄 · 환경변수 3줄 — 그리고 병목은 **부모가 아니라 잎**에 있다. ★
