# (프로젝트 이름)

> ⚠️ **발표가 없습니다. 무엇을 만들었는지는 이 README 로만 전달됩니다.**
> **README 가 곧 점수입니다.** (문서화 품질 2%)
>
> 오늘(10주차)은 1~4번을 채웁니다. 5번과 "한계와 개선 방향" 은 14주차 제출 전에 채웁니다.

## 1. 목적

누구의 어떤 불편을 해결하는가 — 2~3문장

## 2. 기능

- 기능 1
- 기능 2

## 3. 사용 모델과 선택 근거 ★

- 생성 모델: ____________
- 임베딩 모델: __________
- 근거: (4주차 비교표의 어느 항목 때문인가 — 품질 / 속도 / 비용 / 보안 / 오프라인 / VRAM)
- VRAM 계산: ____ GB → 8GB 안에 들어감 ✅

```
   ⚠️ 4주차 1교시의 계산법을 그대로 씁니다.

     생성 LLM (___B Q4)   ≒ ____ GB
     임베딩 모델          ≒ ____ GB
     KV 캐시 + 오버헤드   ≒ ____ GB
     ─────────────────────────────
     합계                 ≒ ____ GB    (8GB 이내여야 합니다)
```

> ⚠️ **설계만 하고 안 돌려본 팀이 13주차 중간 점검에서 무너집니다.**
> 계산만 하지 말고 **실제로 한 번 올려 보십시오.**

## 4. 사용 스택

- **LangChain**: (어떤 구성요소를 쓰는가)
- **LangSmith**: (어디에 추적을 붙일 것인가) ★ **필수 요건입니다**
- LangGraph: (사용 시 — 과제 5 그래프를 골격으로 쓰는 것을 권장 ★)

> ⚠️ **LangSmith 추적은 감점 항목이 아니라 요건입니다.** 안 붙어 있으면 요건 미충족입니다.
> `.env` 에 3줄 넣으면 끝납니다 (6주차 1교시). 지금 붙이십시오.

## 5. 실행 방법

(설치 → 실행까지 **그대로 따라 할 수 있게** 씁니다)

```bash
# 예시
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env      # 키를 채운다
python main.py
```

## 6. 한계와 개선 방향

(14주차 제출 전에 채웁니다. **무엇이 안 되는지 솔직히 쓰는 편이 점수에 유리합니다** ★)

---

## 제출 체크리스트

### 10주차 (오늘) — 명세 2%

- [ ] `README.md` 1~4번
- [ ] `CONTEXT.md` 전체

### 14주차 — 구현 4% + 문서 2%

- [ ] 최종 산출물(코드)
- [ ] `README.md` 6항목 완성
- [ ] ★★ **LangSmith 추적 캡처(필수)**
      — 무료 플랜 보존기간 **14일**. 링크만 제출하면 채점 시점에 소멸됩니다.
      **제출 직전에 반드시 캡처하십시오.**
