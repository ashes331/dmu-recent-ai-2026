# 모델 비교표 — [실습 2 / 실습 4]

> 이름: ____________  측정일: 2026-09-18
>
> ⚠️ **이 표는 버리지 마십시오.** 10주차 미니 프로젝트 명세에서
> *"왜 이 모델을 골랐는가"* 의 근거로 그대로 재사용합니다. 저장소에 커밋하세요.

## 측정 조건

| 항목 | 값 |
|------|-----|
| 프롬프트 | 3주차 `first_chain.py` 와 동일 (`{level}` 눈높이 / `{topic}` 장점 3가지) |
| 입력값 | `level="초보자"`, `topic="파이썬"` |
| `temperature` | 0.2 (두 모델 동일) |
| 내 PC VRAM | ______ GB |

## 비교표 (6항목)

| 항목 | 로컬 gemma3:4b | OpenAI ____________ |
|---|---|---|
| **응답 품질** (1~5 주관) | | |
| **총 소요 시간** (초) | | |
| **첫 토큰까지** (초) | ⏳ 3교시 실습 4에서 채움 | ⏳ |
| **입력 / 출력 토큰** | / | / |
| **비용** (1회 호출) | **0원** | |
| **보안** (데이터 외부 전송) | **없음** ✅ | 전송됨 |
| **오프라인 가능** | **가능** ✅ | 불가 |

> `swap_models.py` 실행 결과의 '측정 요약'을 그대로 옮겨 적으면 됩니다.
> '첫 토큰까지' 칸은 `invoke()` 로 잴 수 없어 비워 둡니다 → `streaming.py` 에서 채웁니다. ★

### 비용 계산

```
비용 = (입력 토큰 × 입력 단가) + (출력 토큰 × 출력 단가)
```

| | 입력 단가 | 출력 단가 |
|---|---|---|
| 수업일 기준 배포 단가 | | |

> 단가를 외우지 마십시오. 외울 것은 **계산 구조** — 입력과 출력의 단가가 다르고,
> **출력이 대체로 더 비싸다**는 점입니다.

### 상용 키를 못 쓴 경우 (대체안)

로컬 2종(`gemma3:1b` vs `gemma3:4b`) 비교로 대체하고, **'비용' 칸을 '메모리 점유'로 치환**합니다.
`ollama ps` 의 `SIZE` 열 값을 그 자리에 넣으면 됩니다. (1교시)

| 항목 | 소형 gemma3:1b | 중형 gemma3:4b |
|---|---|---|
| **메모리 점유** (`ollama ps` SIZE) | | |
| **PROCESSOR** (GPU/CPU 분산) | | |

## 표에서 읽어내기

> **핵심 질문**: 그래서 미니 프로젝트에는 무엇을 쓸 것인가?

| 상황 | 유력한 선택 | 근거 열 |
|---|---|---|
| 개인정보·사내 문서를 다룬다 | 로컬 | 보안 |
| 인터넷이 불안정한 환경 | 로컬 | 오프라인 |
| 복잡한 추론이 필요하다 | 상용 | 품질 |
| 호출량이 매우 많다 | 로컬 또는 소형 상용 | 비용 |
| 8GB VRAM을 넘는 모델이 필요 | 상용 | 1교시 VRAM 계산 ★ |

**내 미니 프로젝트에서 고를 모델과 그 이유 (한 문장):**

```
                                                                          
```
